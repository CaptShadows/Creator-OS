"use client";
import{useEffect,useRef,useState}from"react";
export function ConfirmActionDialog({label,title,name,description,action,fields,tone="default"}:{label:string;title:string;name:string;description:string;action:(form:FormData)=>void|Promise<void>;fields:Record<string,string>;tone?:"default"|"danger"}){const[open,setOpen]=useState(false),trigger=useRef<HTMLButtonElement>(null),confirm=useRef<HTMLButtonElement>(null);const close=()=>{setOpen(false);queueMicrotask(()=>trigger.current?.focus())};useEffect(()=>{if(!open)return;confirm.current?.focus();const onKey=(event:KeyboardEvent)=>{if(event.key==="Escape")close()};document.addEventListener("keydown",onKey);return()=>document.removeEventListener("keydown",onKey)},[open]);return <><button ref={trigger} type="button" className={`min-h-11 text-sm font-bold ${tone==="danger"?"text-red-800":"text-[var(--accent-strong)]"}`} onClick={()=>setOpen(true)}>{label}</button>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4" onMouseDown={event=>{if(event.target===event.currentTarget)close()}}><div role="dialog" aria-modal="true" aria-labelledby="confirm-action-title" className="w-full max-w-lg border border-[var(--line)] bg-white p-6 text-[var(--foreground)] shadow-2xl sm:p-8"><p className="eyebrow">Please confirm</p><h2 id="confirm-action-title" className="display-heading mt-3 text-3xl">{title}</h2><p className="mt-4 text-sm leading-6 text-[var(--muted)]"><strong>{name}</strong> — {description}</p><div className="mt-7 flex flex-wrap justify-end gap-3"><button type="button" className="min-h-12 border border-[var(--line)] px-5 text-sm font-bold" onClick={close}>Cancel</button><form action={action}>{Object.entries(fields).map(([key,value])=><input key={key} type="hidden" name={key} value={value}/>)}<button ref={confirm} className={`min-h-12 px-5 text-sm font-bold text-white ${tone==="danger"?"bg-red-800":"bg-[var(--accent-strong)]"}`}>Confirm</button></form></div></div></div>}</>}
