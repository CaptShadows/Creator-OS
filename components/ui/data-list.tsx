export function DataList({ label, items }: { label: string; items: Array<{ id: string; title: string; detail?: string; status?: React.ReactNode }> }) { return <div role="list" aria-label={label} className="divide-y divide-[var(--line)] border-y border-[var(--line)]">{items.map((item) => <div role="listitem" key={item.id} className="flex min-h-14 items-center justify-between gap-4 py-3"><div><p className="font-semibold">{item.title}</p>{item.detail && <p className="profile-density-secondary mt-1 text-sm text-[var(--muted)]">{item.detail}</p>}</div>{item.status}</div>)}</div>; }
