export function ErrorState({ title, description, retry }: { title: string; description: string; retry?: () => void }) { return <div role="alert" className="border border-[var(--accent)] bg-[var(--accent-soft)] p-6"><p className="eyebrow">Needs attention</p><h2 className="display-heading mt-2 text-3xl">{title}</h2><p className="mt-3 leading-7 text-[var(--muted)]">{description}</p>{retry && <button type="button" onClick={retry} className="primary-action mt-5 min-h-12 px-5 text-sm font-bold">Try again</button>}</div>; }
