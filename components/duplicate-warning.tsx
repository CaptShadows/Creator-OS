import Link from "next/link";
export function DuplicateWarning({matches,children,cancelHref}:{matches:string|undefined;children:React.ReactNode;cancelHref:string}){if(!matches)return null;let rows:{id:string;name:string;href:string;reason:string}[]=[];try{rows=JSON.parse(matches)}catch{return null}return <div role="alert" className="border border-amber-300 bg-amber-50 p-5"><p className="eyebrow">Possible duplicate</p><h2 className="display-heading mt-2 text-2xl">Check before creating</h2><p className="mt-2 text-sm text-[var(--muted)]">Creator OS found an owner-scoped name match. Nothing has been created yet.</p><ul className="mt-4 grid gap-2">{rows.map(x=><li key={x.id}><Link className="font-bold text-[var(--accent-strong)]" href={x.href}>Open {x.name}</Link><span className="text-sm text-[var(--muted)]"> · {x.reason}</span></li>)}</ul><div className="mt-5 flex flex-wrap items-end gap-3">{children}<Link className="min-h-12 border border-[var(--line)] bg-white px-5 py-3 text-sm font-bold" href={cancelHref}>Cancel</Link></div></div>}
