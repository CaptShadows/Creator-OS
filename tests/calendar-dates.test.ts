import{describe,it,expect}from"vitest";import{calendarDays,calendarRange,campaignRangeDays,dateInputToUtc,dayKey,parseCalendarDate,shiftCalendar}from"@/lib/calendar/dates";describe("calendar dates",()=>{it("parses date-only values without local timezone drift",()=>expect(dayKey(parseCalendarDate("2026-08-01"))).toBe("2026-08-01"));it("uses half-open month boundaries",()=>{const r=calendarRange(parseCalendarDate("2026-12-15"),"month");expect(dayKey(r.start)).toBe("2026-12-01");expect(dayKey(r.end)).toBe("2027-01-01");expect(calendarDays(r.start,r.end)).toHaveLength(31)});it("builds and shifts a seven-day week",()=>{const anchor=parseCalendarDate("2026-08-14"),r=calendarRange(anchor,"week");expect(calendarDays(r.start,r.end).map(dayKey)).toEqual(["2026-08-10","2026-08-11","2026-08-12","2026-08-13","2026-08-14","2026-08-15","2026-08-16"]);expect(dayKey(shiftCalendar(anchor,"week",1))).toBe("2026-08-21")});it("anchors stored date-only scheduling at UTC noon",()=>expect(dateInputToUtc("2026-08-13").toISOString()).toBe("2026-08-13T12:00:00.000Z"));it("expands a campaign across its inclusive start and due dates",()=>{const visible=calendarRange(parseCalendarDate("2026-08-14"),"month"),days=campaignRangeDays(parseCalendarDate("2026-08-17"),parseCalendarDate("2026-08-21"),visible.start,visible.end);expect(days.map(x=>dayKey(x.date))).toEqual(["2026-08-17","2026-08-18","2026-08-19","2026-08-20","2026-08-21"]);expect(days.map(x=>x.position)).toEqual(["start","middle","middle","middle","end"]);expect(days.at(-1)?.isDeadline).toBe(true)});});
