import { describe,expect,it } from "vitest";import { canTransitionSample,nextSampleStatus,statusesForFilter } from "@/lib/samples/lifecycle";import { quickSampleSchema } from "@/lib/samples/contracts";
describe("sample workflow",()=>{it("moves through adjacent explicit states",()=>{expect(nextSampleStatus("arrived")).toBe("content_needed");expect(canTransitionSample("arrived","content_needed")).toBe(true);expect(canTransitionSample("requested","shipped")).toBe(false)});it("maps action filters",()=>{expect(statusesForFilter("pending")).toEqual(["requested","pending","approved"]);expect(statusesForFilter("needs_content")).toEqual(["content_needed"])});it("supports quick capture for a new or existing product",()=>{expect(quickSampleSchema.safeParse({newProductName:"Serum",sourcePlatform:"TikTok",productId:""}).success).toBe(true);expect(quickSampleSchema.safeParse({newProductName:"",productId:""}).success).toBe(false)});});
