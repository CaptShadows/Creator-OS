import{describe,expect,it}from"vitest";import{createBrandDealSchema,dealDeliverableSchema,editBrandDealSchema}from"@/lib/brand-deals/contracts";import{dealProgress,outstanding}from"@/lib/brand-deals/calculations";
describe("brand deals",()=>{it("validates a minimal distinct commercial record",()=>{expect(createBrandDealSchema.parse({brandName:"Acme",title:"Fall launch",source:"direct_email",dealType:"paid"})).toMatchObject({brandName:"Acme",title:"Fall launch"})});it("tracks quantity-aware deliverable progress",()=>{expect(dealProgress([{status:"approved",quantity:2},{status:"in_progress",quantity:1}])).toEqual({completed:2,total:3,percent:67})});it("calculates cash outstanding without counting gifted value",()=>{expect(outstanding(100000,25000,40000)).toBe(60000)});it("rejects cross-record identifiers that are not UUIDs",()=>{expect(()=>dealDeliverableSchema.parse({brandDealId:"wrong",title:"Reel",quantity:1,status:"not_started",approvalStatus:"not_submitted"})).toThrow()});it("supports commercial rights and payment fields",()=>{const x=editBrandDealSchema.parse({id:"00000000-0000-4000-8000-000000000001",title:"Deal",source:"direct_email",dealType:"paid",status:"contracted",priority:"2",currency:"USD",paymentStatus:"expected",amountReceivedCents:"50",contractSigned:"on",organicRights:"on",paidUsage:"on"});expect(x).toMatchObject({contractSigned:true,organicRights:true,paidUsage:true,amountReceivedCents:5000})})});
