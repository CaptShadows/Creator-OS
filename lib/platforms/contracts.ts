import{z}from"zod";import{platformSchema}from"@/lib/domain/contracts";const text=z.string().trim().max(300).optional().transform(v=>v||null);export const createPlatformAccountSchema=z.object({platform:platformSchema,displayName:z.string().trim().min(1).max(200),handle:text});export const editPlatformAccountSchema=z.object({accountId:z.uuid(),displayName:z.string().trim().min(1).max(200),handle:text,active:z.enum(["true","false"]).transform(v=>v==="true")});
