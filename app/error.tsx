"use client";
export default function ErrorBoundary({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <section className="mx-auto max-w-xl py-20 text-center"><p className="text-sm font-bold uppercase tracking-[0.16em] text-[var(--accent)]">Something went wrong</p><h1 className="mt-3 text-4xl font-bold">Creator OS hit a snag.</h1><p className="mt-4 leading-7 text-[var(--muted)]">Your work has not been intentionally changed. Try loading this view again.</p><button type="button" onClick={reset} className="mt-8 min-h-12 rounded-xl bg-[var(--accent)] px-5 text-sm font-bold text-white">Try again</button></section>; }
