import Link from "next/link";import { notFound } from "next/navigation";import { requireOwner } from "@/lib/auth/server";import { getSampleWorkspace } from "@/lib/samples/repository";import { nextSampleStatus,previousSampleStatus,sampleStatusLabels } from "@/lib/samples/lifecycle";import { PageHeader } from "@/components/ui/page-header";import { SurfaceCard } from "@/components/ui/surface-card";import { StatusBadge } from "@/components/ui/status-badge";import { transitionSampleAction,updateSampleAction } from "../../actions";
export const dynamic="force-dynamic";const date=(d:Date|null)=>d?.toISOString().slice(0,10)??"";export default async function SamplePage({params}:{params:Promise<{id:string}>}){const o=await requireOwner();const data=await getSampleWorkspace(o.id,(await params).id);if(!data)notFound();const s=data.sample;const previous=previousSampleStatus(s.status),next=nextSampleStatus(s.status);return <section className="space-y-8"><Link href="/products" className="text-sm font-bold text-[var(--accent-strong)]">← Products & samples</Link><PageHeader eyebrow="Sample" title={data.productName} action={<StatusBadge>{sampleStatusLabels[s.status]}</StatusBadge>}/><SurfaceCard><h2 className="display-heading text-2xl">Lifecycle</h2><div className="mt-4 flex flex-wrap gap-3">{previous&&<Transition id={s.id} to={previous} label={`Back to ${sampleStatusLabels[previous]}`}/>} {next&&<Transition id={s.id} to={next} label={`Mark ${sampleStatusLabels[next]}`}/>}</div><div className="mt-5 grid gap-2 text-sm text-[var(--muted)]"><p>Requested: {s.requestedAt?.toLocaleDateString()||"Unknown"}</p><p>Approved: {s.approvedAt?.toLocaleDateString()||"Not yet"}</p><p>Shipped: {s.shippedAt?.toLocaleDateString()||"Not yet"}</p><p>Received: {s.receivedAt?.toLocaleDateString()||"Not yet"}</p></div></SurfaceCard><SurfaceCard><h2 className="display-heading text-2xl">Sample details</h2><p className="mt-2 text-sm text-[var(--muted)]">Add shipping and work connections when they become useful.</p><form action={updateSampleAction} className="mt-5 grid gap-4 md:grid-cols-2"><input type="hidden" name="sampleId" value={s.id}/><Input label="Source" name="sourcePlatform" defaultValue={s.sourcePlatform??""}/><Input label="Expected delivery" name="expectedDeliveryAt" type="date" defaultValue={date(s.expectedDeliveryAt)}/><Input label="Tracking/reference" name="trackingReference" defaultValue={s.trackingReference??""}/><Select label="Content" name="contentId" defaultValue={s.contentId??""} empty="Link later" options={data.contentOptions}/><Select label="Campaign" name="campaignId" defaultValue={s.campaignId??""} empty="Link later" options={data.campaignOptions}/><Input label="Notes" name="notes" defaultValue={s.notes??""}/><button className="primary-action min-h-12 px-5 text-sm font-bold md:col-span-2 md:justify-self-start">Save sample</button></form></SurfaceCard></section>}
function Transition({id,to,label}:{id:string;to:string;label:string}){return <form action={transitionSampleAction}><input type="hidden" name="sampleId" value={id}/><input type="hidden" name="to" value={to}/><button className="min-h-11 border border-[var(--line)] px-4 text-sm font-bold">{label}</button></form>}function Input({label,...props}:{label:string;name:string;type?:string;defaultValue?:string}){return <label className="grid gap-2 text-sm font-bold">{label}<input className="min-h-12 border border-[var(--line)] bg-white px-3 font-normal" {...props}/></label>}function Select({label,options,empty,...props}:{label:string;name:string;defaultValue?:string;empty:string;options:{id:string;name:string}[]}){return <label className="grid gap-2 text-sm font-bold">{label}<select className="min-h-12 border border-[var(--line)] bg-white px-3 font-normal" {...props}><option value="">{empty}</option>{options.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>}
